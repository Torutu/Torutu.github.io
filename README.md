# Portfolio

Personal portfolio site displaying my projects.

## Tech

- React + TypeScript + HTML + CSS
- Deployed on Azure

## Makefile

```bash
make install   # install dependencies
make start     # start dev server at localhost:3000
make build     # production build → build/
```
